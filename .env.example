# GitHub API token (optional - increases rate limits)
GITHUB_TOKEN=

# GitHub webhook secret (optional - for cache invalidation)
GITHUB_WEBHOOK_SECRET=

# Image upload API authentication token (required for /api/images/upload)
# Generate with: openssl rand -hex 32
IMAGE_UPLOAD_TOKEN=

# Local cache settings (optional)
USE_LOCAL_CACHE=false
LOCAL_CACHE_PATH=
